# {{ display_name }}

This is the README for the **{{ display_name }}** agent created using the Create Agentverse Agent tool.

## Description

{{ agent_description }}
{% if not agentverse_api_key %}

## Environment Variables
- Add your `AGENT_VERSE_API` key in the `.env` file for better support.
{% endif %}

## Setup
- Use `make install` to install dependencies.

## Running the Agent
- Use `make {{ "run" if agentverse_api_key else "dev" }}` to start the agent.
