# {{ agent.name }}

**Handle:** `{{ agent.handle }}`

{{ agent.description }}

---

Profile readme for Agentverse. Published on registration when `AGENTVERSE_API_KEY` is set in `.env`.

Short summary on Agentverse uses `agent.yml` → `agent.description`. Edit this file for the longer profile page.
