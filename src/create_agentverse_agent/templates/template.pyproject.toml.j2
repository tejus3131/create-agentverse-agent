[tool.poetry]
name = "{{ safe_name }}"
version = "0.1.0"
description = "{{ agent_description }}"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10,<3.15"
uagents = "^0.23.4"
python-dotenv = "^1.2.1"
